//
//  Autenticity.swift
//  StudyHUB
//
//  Created by Miguel Silva da Rocha Junior on 4/20/17.
//  Copyright © 2017 StudyHUB. All rights reserved.
//

import Foundation
import CoreData

class AutenticityDAO {
    
    /*
    // 1. Add
    static func adicionar(_ filme: Filme) -> Bool {
        return CoreDataManager.adicionar(filme)
    }
    // 2. Remover
    
    static func remover(_ filme: Filme) -> Bool {
        return CoreDataManager.remover(filme)
    }
    
    // 3. Buscar
    static func buscarTodos() -> [Filme]{
        var resultados = [Filme]()
        
        let request: NSFetchRequest<Filme> = Filme.fetchRequest()
        
        request.sortDescriptors = [NSSortDescriptor(key: "titutlo", ascending: true)]
        
        do{
            try resultados = CoreDataManager.getContext().fetch(request)
        } catch let error {
            print ("Ero ao buscar: \(error)")
        }
        
        return resultados
    }
 
 */
    
}
